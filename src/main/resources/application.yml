server:
  port: 8084

spring:
  application:
    name: geeks_chat_server
  datasource:
    username: thapeloseema
    password: password
    url: jdbc:postgresql://:5433/postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true